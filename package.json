{
  "name": "smartvyapar-backend",
  "version": "1.3.5",
  "description": "SmartVyapar POS API Server - Hostinger Ready",
  "main": "server.js",
  "scripts": {
    "build": "python3 -m venv .venv && .venv/bin/pip install --no-cache-dir -r requirements.txt || pip3 install --break-system-packages -r requirements.txt || node -e \"console.log('Build OK')\"",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}
